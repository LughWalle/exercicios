// Parece que algo aconteceu com essas strings. Você consegue descobrir como limpar o caos?
// Escreva uma função que recebe dois parametros
// O primeiro com o caracter % no meio da palavra
// O segundo a palavra deve ser invertida e com o caracter %
// Remova as % e reorganize o segundo parametro

// exemplos:
// entrada > 'java', 'tpi%rcs' | saida > 'Javascript'

// entrada > 'c%ountry', 'edis' | saida > 'Countryside'

// entrada > 'down', 'nw%ot' | saida > 'Downtown'

// Una essas strings de forma que elas formem as seguintes palavras: 'Javascript', 'Countryside' e 'Downtown'


// Dicas: Você pode dar uma olhada nos métodos básicos de string JS como replace(), split(), slice() etc